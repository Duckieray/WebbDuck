// WebbDuck browser entrypoint.
// Keep the main Studio application and small host-level settings extensions
// independently auditable while preserving the existing /ui/app.js URL.
import './app_main.js';
import './modules/ProviderCredentialsSettings.js';
